// The Chords Pro app is a static HTML/CSS/JS PWA served from /public.
// This React stub does nothing; it exists only so CRA's dev/build pipeline keeps working.
function App() {
  return null;
}

export default App;
